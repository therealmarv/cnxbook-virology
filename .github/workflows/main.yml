name: A workflow to build books using Enki
on: push
jobs:
  build:
    name: Build book
    runs-on: ubuntu-latest
    container: samuelklutse/enki_epub:latest
    permissions:
      deployments: write
      packages: write
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: List files in the repository
        run: |
          ls -alt /__w/${{ event.repository.name }}/${{ event.repository.name }}
      - name: Build Book
        run: |
          echo 'Initializing Book Build'
          cd /github
          git clone https://github.com/openstax/enki
          cd enki
          git checkout epub 
          TRACE_ON=1 /github/enki/enki-in-container --command all-git-epub --repo-dir ${{ github.workspace }} --style=dummy
      - name: Set Tag as and Env variable
        run: echo "TAG=$(date +'%Y%m%d.%H%M%S')" >> $GITHUB_ENV
#      - uses: ncipollo/release-action@v1
#        with:
#          artifacts: /tmp/virology.epub
#          omitBody: True
#          tag: ${{ env.TAG }}
#      - uses: actions/upload-artifact@v3
#        with:
#          name: ${{ github.event.repository.name }}
#          path: /tmp/virology.epub
#          if-no-files-found: error

